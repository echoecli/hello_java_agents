server:
  port: 1314
  servlet:
    encoding:
      charset: UTF-8
      enable: true
      force: true

spring:
  ai:
    dashscope:
      #阿里云百炼 apiKey
      api-key: sk-7920241a9b354c2b8b63ae49a4136166
      chat:
        options:
          model: qwen-plus
      embedding:
        options:
          model: text-embedding-v3
          dimensions: 1024
    vectorstore:
      redis:
        initialize-schema: true
        prefix: zwq_rag_prefix
        index-name: zwq_rag_index
  data:
    redis:
      host: 192.168.110.22
      port: 6379

# 图生视频配置
video:
  generation:
    # 阿里云百炼图生视频API Key（可与上面的api-key相同或单独配置）
    api-key: sk-7920241a9b354c2b8b63ae49a4136166
    # 图生视频接口地址
    base-url: https://dashscope.aliyuncs.com/api/v1/services/aigc/video-generation/video-synthesis